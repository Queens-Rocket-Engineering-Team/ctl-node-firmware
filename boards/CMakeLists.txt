idf_component_register(
    SRC_DIRS
    "${BOARD}"
    INCLUDE_DIRS
    "."
    "${BOARD}"
    REQUIRES
    esp_common
    controls
    sensors
    pid
    freertos
    esp_driver_ledc
    esp_driver_gpio
    esp_driver_pcnt
)